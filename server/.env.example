# AI concierge backend env
ANTHROPIC_API_KEY=sk-ant-...           # required — your Claude API key
ALLOWED_ORIGIN=https://banksy1224.github.io   # optional — lock CORS to your site (default: *)
# PORT is provided by Railway automatically; defaults to 8787 locally.
